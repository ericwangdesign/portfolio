---
// Microsoft Clarity — behavioral analytics (heatmaps, scroll maps, session
// recordings, rage/dead-click detection). Free and unlimited.
//
// To activate: create a project at https://clarity.microsoft.com for
// ericwangdesign.com, then copy the Project ID from
// Settings → Overview (it's a short string like "abcd1234ef") and paste it
// below. Until a real ID is set, this component renders nothing — so it is
// safe to build and deploy with the placeholder in place.
const CLARITY_PROJECT_ID = "xprgtw49pv";
const enabled = CLARITY_PROJECT_ID !== "PASTE_CLARITY_PROJECT_ID";
---

{
  enabled && (
    <script is:inline define:vars={{ CLARITY_PROJECT_ID }}>
      (function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            (c[a].q = c[a].q || []).push(arguments);
          };
        t = l.createElement(r);
        t.async = 1;
        t.src = "https://www.clarity.ms/tag/" + i;
        y = l.getElementsByTagName(r)[0];
        y.parentNode.insertBefore(t, y);
      })(window, document, "clarity", "script", CLARITY_PROJECT_ID);
    </script>
  )
}
